# 3 mai import
#  1.if x any y are numeric then draw scatter plot or relationship
#  2.if is one column a category then draw bar plot or count plot
#  3.if want ot see a distribution shape then draw histogram or KDE plot or Box plot


import pandas as pd 
import numpu as np
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_absolute_error, root_mean_squared_error, mean_squared_error